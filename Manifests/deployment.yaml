apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: akswebapp
  name: akswebapp
  namespace: akswebapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: akswebapp
  template:
    metadata:
      labels:
        app: akswebapp
    spec:
      containers:
        image: jpcontainerregisternew.azurecr.io/https://github.com/jitenderp1234/AKS-deployment:14
        name: akswebapp
        envFrom:
        - configMapRef:
            name: env-config
